<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launch theme: hands off to the Compose UI as soon as the first frame is ready. -->
    <style name="Theme.SubKan" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <!--
          The mark is purple on white, so it needs white behind it on the splash too — otherwise it
          sits on the dark splash background in dark theme and all but disappears.
        -->
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.SubKan.Content</item>
    </style>

    <style name="Theme.SubKan.Content" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
