# 変更履歴

このファイルの形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

## [2.0.0] - 2026-08-04

Flutter（Dart + Hive + Riverpod）から Android ネイティブ（Kotlin + Jetpack Compose + Room）へ
全面的に書き換えました。Material Design 3 を本格的に導入することが目的です。

**機能は v1.0.2 から変えていません。** できていたことは引き続きできます。移行の詳細と対応表は
[docs/migration-from-flutter.md](docs/migration-from-flutter.md) を参照してください。

### 破壊的変更

- **データは引き継がれません。** 保存形式が Hive から Room へ変わり、アプリ ID も
  `com.example.subscription_manager` から `com.subkan` になったため、v2.0.0 は新規インストール
  扱いになります。
- カード管理画面のスワイプ削除を、削除ボタン＋確認ダイアログに変更しました（取り消しは引き続き
  可能）。スクリーンリーダーから操作できるようにするためです。
- **「済」バッジを廃止しました。** 一覧のカウントダウンと「支払日順」の並びも支払いサイクルから
  次回請求日を導出するようになったため、次回請求日が今日より前になることがなくなり、「済」は
  到達しない状態になりました。バッジは常に「次の請求まであと何日か」を表しています。

### 追加

- **金額が変動するものの登録。** 光熱費のように毎月の金額が変わるものを登録できるようにしました。
  金額は**任意**になり、次の 3 通りが選べます。
  - **件名だけ登録** — 金額欄は空のまま。一覧では `—` と表示され、月額合計には含まれません
    （0 円としては数えません）。
  - **概算で登録** — 目安の金額を入れて「金額は概算（約）」をオン。`約¥4,000` と表示されます。
  - **確定金額で登録** — 従来どおり。
  概算を 1 件でも含む通貨の合計は、金額の前ではなくラベル横の「概算含む」チップで示します。
- **金額の表記を選べるように。** 設定 →「金額の表記」で、記号（`¥1,480` / `$20`、既定）と
  日本語（`1,480円` / `20ドル`）を切り替えられます。記号表記のときは「約」との間に空白を入れます
  （`約 ¥4,000`）。
- **「約」表示の切り替え。** 設定 →「概算に「約」を表示」でオン／オフできます（既定はオン）。
  オフにしても保存されているデータは変わりません。
- **支払日の通知。** 前日の夜と当日の朝に、その日に引き落とされるサブスクをまとめてお知らせします
  （「明日は Netflix の支払日です」「明日は 3 件の支払日です」）。前日・当日それぞれについて
  ON/OFF と通知時刻を個別に設定できます。既定は前日 20:00・当日 8:00 で、どちらも ON です。
  - 同じ日に複数の支払いがあっても通知は 1 通にまとまります。
  - 通知は支払いサイクルから次回請求日を導出するため、「毎月5日」のサブスクは毎月通知されます
    （保存されている支払日は書き換えません）。
  - 同じ日の件数が 7 件以上になる場合は、名前を並べず件数と合計だけを表示します。
- **通知権限のリクエスト。** 初回起動時に 1 度だけ求めます。通知機能より前からアプリを使っている
  場合も、次に開いたときに求められます。以降はアプリを開くたびに許可状態を再確認し、端末の設定
  から許可された場合もその時点で通知が有効になります。許可されていないまま通知が ON の場合は、
  設定画面に警告を表示して端末の設定へ誘導します。
- ダイナミックカラー（Android 12 以降）。壁紙由来の配色を使う設定。**既定はオフ**で、オフのまま
  なら従来と同じ紫のテーマです。
- カードの並べ替えに、ドラッグに加えて上／下ボタンを追加。ドラッグは TalkBack から到達できない
  ためです。
- サブスク削除時の取り消し（スナックバー）。
- 空状態の説明文（サブスク未登録時・カード未登録時）。

### 変更

- 書体を端末のシステムフォントに変更（従来は Inter / Outfit をダウンロード）。日本語・英数字・
  絵文字を 1 つのフォントで正しく描き分けられ、フォントスケール設定にも対応するためです。金額の
  見た目はウェイトと字間で表現しています。
- 次回支払日をエポック日で保存するようにしました。タイムゾーンが変わっても「毎月5日」が
  ずれません。
- 一覧の並び替えを SQL ではなく `core/model` で行うようにしました。日本語を含む名称の
  大文字小文字を無視した比較のためと、ユニットテストで守るためです。

- **アプリアイコンを刷新。** 支払いの繰り返しを表す新しいマークに差し替えました。

### 開発面

- `CLAUDE.md` と `.claude/`（rules / skills / agents / hooks）を整備。
- `docs/architecture.md`・`docs/migration-from-flutter.md`・`docs/roadmap.md` を追加。
- ユニットテストを追加（月額合計・並び替え・支払い日判定・ロゴ URL 解決・カード削除の 2 経路）。
- GitHub Actions の CI（コンパイル＋テスト）を追加。リリースワークフローを Gradle 向けに更新。

## [1.0.2] - 2026-07-06

### 修正

- タブをスワイプした際に、カードごとの月額合計が更新されないことがある問題を修正。

## [1.0.1] - 2026-06-07

### 変更

- カード・設定・タブ操作まわりの UI を改善。

## [1.0.0] - 2026-06-07

- 初回リリース。
