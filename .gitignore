*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Release signing. Never commit any of these — this repo is public, and a leaked signing key
# cannot be revoked. .jks is what keytool produces by default, so it matters more than .keystore.
*.keystore
*.jks
*.p12
keystore.properties
!debug.keystore
/kotlin-js-store
.kotlin/
